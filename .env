# Vite production environment variables
# Set the API base URL in production. If you serve frontend and backend from the same origin (recommended), set this to the site origin.
VITE_API_URL="https://shyro.ovh"
